/**
 * (auth) group layout
 * Individual auth pages own their full-screen presentation.
 */
export const metadata = { title: 'Sign In' };

export default function AuthLayout({ children }) {
  return children;
}
